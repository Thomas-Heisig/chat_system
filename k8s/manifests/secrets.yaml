# This is a template - DO NOT commit real secrets!
# Use: kubectl create secret generic chat-system-secrets --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: chat-system-secrets
  namespace: chat-system
type: Opaque
stringData:
  APP_SECRET_KEY: "CHANGE-THIS-SECRET-KEY-IN-PRODUCTION"
  DATABASE_URL: "postgresql://user:password@postgres:5432/chat_system"
  REDIS_URL: "redis://redis:6379/0"
  # Optional: Add more secrets as needed
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""
